# 힉스필드 이미지 생성 — 캐릭터 프롬프트 가이드

초등학생 대상 학습 서비스의 4종 캐릭터(다정파·기분파·분석파·인상파)를 힉스필드로
일관성 있게 생성하기 위한 팀 공용 프로젝트.
**작업 중 얻은 개선사항은 반드시 파일에 반영해 커밋한다.**

## 기본 설정
- 대상: 초등학생 — 항상 밝고 친근하고 안전한 톤. 무섭거나 위협적인 요소 절대 금지
- 렌더 방식: **2D(앱/웹 UI용 플랫 벡터)** 또는 **3D(굿즈용 무광 피규어)** — 씬마다 하나만 선택
- 비율: 용도마다 다름, 씬 프롬프트에 매번 명시. **팝업 기본값은 1280x800**
- 텍스트/말풍선이 필요한 씬은 **텍스트를 직접 생성하지 않는다** — 위치와 크기만 지정한 빈 영역으로 처리

## 워크플로우 (필수 순서)
1. 한국어 요청 분석 → 캐릭터 등장 여부, 인원수(1~4명), 용도(앱/SNS/굿즈) 파악
2. 참조 파일을 먼저 읽는다:
   - 캐릭터 이름 등장 → `style/characters.md` + `templates/character-scene.md`
3. 새 요청이 과거 작업과 비슷하면 `prompts/`에서 유사 로그를 검색해 재사용·변형한다.
4. `templates/character-scene.md`의 골격대로 **영문 구조화 프롬프트** 작성
5. 생성 전, 변환 결과를 **3~4줄 요약(한국어 의역 + 핵심 영문 키워드)**으로 보여주고 진행.
   사용자가 "전문 보여줘" 하면 전체 프롬프트를 보여준다.
6. 결과가 확정(사용자 OK)되면 `prompts/YYYY-MM-DD-슬러그.md`로 로그 저장

## Element 바인딩 (캐릭터)
- 캐릭터 이름이 하나라도 등장하면 **반드시 `style/characters.md`를 먼저 읽는다.**
- 등록 캐릭터는 프롬프트 **맨 앞**에 `<<<element-id>>>` 토큰으로 바인딩한다.
  여러 명이면 전원 나열: `<<<id1>>> <<<id2>>> ...`
  각 캐릭터는 렌더 방식(2D/3D)에 맞는 Element ID를 사용한다 (`style/characters.md` 표 참고).
- Element 바인딩 + **텍스트 외형 묘사 둘 다** 넣는다 (이중 안전장치).
  외형 묘사는 `style/characters.md`의 영문 블록을 그대로 복사해 쓴다 (즉석 재작성 금지).
- 목록에 없는 이름 → 생성하지 말고 먼저 확인.

## 프롬프트 구조 (모든 생성 공통)
서술 순서: ① 주인공 → ② 소품 → ③ 텍스트 예약 영역(있다면) → ④ 환경/배경 → ⑤ 카메라·구도 →
⑥ 스타일(2D/3D 스위치) → ⑦ Critical conditions → ⑧ Negative
- 빈 배경을 앞에 쓰면 주인공이 빠진다. **주인공 먼저.**
- 복잡한 장면(캐릭터 2명 이상, 제약 많음) → 마크다운 섹션형(`## Scene / ## Cast / ## Critical conditions / ## Negative`)으로 작성. `templates/character-scene.md` 골격 사용.
- 한 번이라도 잘못 나왔던 요소는 Critical conditions에 번호로 명시하고 **Negative에도 중복 기재**.
- 텍스트가 필요한 씬은 `templates/character-scene.md`의 "Text Reserved Area" 패턴 사용 — 실제 글자는 절대 생성하지 않는다.

## 학습 루프 (중요 — 이 프로젝트의 핵심)
재수정 피드백을 받으면 그 프롬프트만 고치고 끝내지 말 것:
1. **원인 규칙을 파일에 영구 반영한다.**
   - 특정 캐릭터 문제 → `style/characters.md` 해당 캐릭터의 "항상 적용되는 제약"에 추가
   - 장면·구도·텍스트 영역·소품 문제 → `templates/character-scene.md`에 추가
2. 반영했으면 "규칙을 ○○ 파일에 반영했다"고 사용자에게 알린다.
3. Git으로 관리하는 프로젝트이므로, 반영 후 **커밋(+ 요청 시 푸시)**한다.

## 프롬프트 로그 (`prompts/`)
- 파일명: `YYYY-MM-DD-슬러그.md`
- 내용: 한국어 요청 원문 / 최종 영문 프롬프트 전문 / 사용 element / 렌더방식·비율 / 시행착오 메모
- 포맷은 `prompts/README.md` 참조
